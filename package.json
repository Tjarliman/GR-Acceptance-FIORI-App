{
  "name": "zmm.gracceptance",
  "version": "0.0.1",
  "description": "Engine and Frame Acceptance - GR Movement Type 301",
  "private": true,
  "scripts": {
    "start": "fiori run --open index.html",
    "start-mock": "fiori run --config ./ui5-mock.yaml --open index.html",
    "build": "ui5 build preload --clean-dest --config ui5.yaml --exclude-task=generateCachebusterInfo",
    "deploy": "npm run build && fiori deploy --config ui5-deploy.yaml && rimraf archive.zip",
    "deploy-test": "npm run build && fiori deploy --config ui5-deploy.yaml --testMode true"
  },
  "devDependencies": {
    "@sap/ux-ui5-tooling": "*",
    "@ui5/cli": "^3.0.0",
    "rimraf": "^5.0.0"
  }
}
